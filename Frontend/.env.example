# ============================================================================
# FastReactCMS - Frontend Environment Configuration
# ============================================================================
# Copy this file to .env and update with your values
# All Vite environment variables must be prefixed with VITE_

# ============================================================================
# BACKEND API CONFIGURATION
# ============================================================================
# Backend API URL
# CRITICAL: Use "localhost", NOT "127.0.0.1"
# Cookie-based authentication requires matching domain names
# Frontend and backend must use same domain (both localhost or both production domain)

# Development (default):
VITE_API_URL=http://localhost:8100

# Production (update with your actual API domain):
# VITE_API_URL=https://api.yourdomain.com
# OR if API is on same domain:
# VITE_API_URL=https://yourdomain.com

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================
# These are optional and can be uncommented if needed

# Application Name (displayed in UI)
# VITE_APP_NAME=FastReactCMS

# Application Version (displayed in footer/about)
# VITE_APP_VERSION=1.0.0

# Google Analytics ID (if using analytics)
# VITE_GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX

# Google AdSense Publisher ID (if using AdSense)
# VITE_ADSENSE_PUBLISHER_ID=ca-pub-XXXXXXXXXXXXXXXX

# ============================================================================
# PRODUCTION DEPLOYMENT CHECKLIST
# ============================================================================
# Before deploying to production, ensure:
# [ ] VITE_API_URL points to production backend (HTTPS required)
# [ ] Backend CORS_ORIGINS includes production frontend domain
# [ ] Both frontend and backend use matching domain names
# [ ] HTTPS is enabled (required for secure cookies)
# [ ] Build output is served from production server
#
# Build command: npm run build
# Build output: dist/
#
# See README.md for deployment guides.
# ============================================================================
